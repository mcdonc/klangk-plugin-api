name: klangk_plugin_api
description: API package for Klangk plugins — plugin base classes and the workspace-services host contract.
publish_to: none
version: 0.5.1

environment:
  sdk: ^3.6.0
  flutter: ^3.27.0

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  web: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
